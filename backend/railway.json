{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "numReplicas": 1,
    "startCommand": "php artisan migrate --force && php artisan db:seed --class=AddMunicipalSchoolsSeeder --force && php artisan db:seed --class=FixPrivateSchoolNamesSeeder --force && php artisan db:seed --class=UpdateMunicipalGroupSeeder --force && php artisan storage:link --force && php artisan octane:start --server=roadrunner --host=0.0.0.0 --port=$PORT --workers=4 --max-requests=1000",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
